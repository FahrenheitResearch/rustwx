Operational radar palette presets used by `rustwx-radar`.

The `.pal` files in this directory are copied from
`FahrenheitResearch/open-dealiasing-algorithms` and kept as data assets rather
than a vendored code dependency. The source repository is MIT licensed; its
license text is preserved in `LICENSE.open-dealiasing-algorithms`.
