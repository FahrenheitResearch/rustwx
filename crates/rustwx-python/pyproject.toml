[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "rustwx-python"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
module-name = "rustwx_python"
features = ["python"]
